# Core
httpx>=0.27.0
python-dotenv>=1.0.0
replicate>=0.25.0

# API server (dashboard)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Production broker (uncomment when ready to go live)
# alpaca-py>=0.20.0
