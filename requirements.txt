# ── Flask core ──────────────────────────────────────────────
flask>=3.0.0
gunicorn>=21.2.0
python-dotenv>=1.0.0

# ── Bot dependencies ─────────────────────────────────────────
httpx>=0.27.0
replicate>=0.25.0          # Llama-3 70B via Replicate
exchange-calendars>=4.5    # NYSE market hours (MarketHoursGuard)
feedparser>=6.0.10         # RSS trending feed parser

# ── Optional data providers ──────────────────────────────────
# finnhub-python>=2.4.18   # uncomment if using FINNHUB_API_KEY
# newsapi-python>=0.2.7    # uncomment if using NEWSAPI_KEY

# ── Optional live broker ─────────────────────────────────────
# alpaca-py>=0.20.0        # uncomment for live PRODUCTION=true